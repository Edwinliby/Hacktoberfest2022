#include <iostream>
using namespace std;
// Perimeter of a Rectangular Fence is 2(w+h),w = width,h = height
int main()
{
	int64_t t;
	cin >> t;
	while (t > 0)
	{
		int64_t N, W;//N si the number of Samples and W is the width
		cin >> N >> W;
		int a[N];
		for (int i = 0; i < N; i++)
		{
			cin >> a[i];
		}
		t--;
	}
	
	return 0;
}